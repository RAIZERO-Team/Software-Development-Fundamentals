create database testtt20;
use testtt20;

create table player (
player_id int,
name varchar(255)
);

insert into player(player_id, name) values (19, 'zizo');
insert into player(player_id, name) values (10, 'sheka');

select * from users;
